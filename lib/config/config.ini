[Log]
; Setze diesen Wert auf `true`, damit Debug-Ausgaben am Bildschirm ausgegeben werden.
debug = true
; Setze diesen Wert auf `true`, damit das Anlegen einer Debug-Log Datei erzwungen wird.
debugFileLog = true
